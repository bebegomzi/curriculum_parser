

------

# 교육과정 편성표 해석 및 변환 스크립트 핸드아웃

## 1. 표의 성격

이 파일은 고등학교 3개년 교육과정 편성표이다.
원본 엑셀은 사람이 보기 좋게 병합 셀과 다층 헤더를 사용하지만, LLM이나 코드가 읽기에는 부적합하다.

따라서 스크립트는 원본 `.xlsx`를 읽어 다음 CSV로 변환한다.

- `*_courses_llm.csv`
  과목 1개를 1행으로 정리한 핵심 표
- `*_selection_groups_llm.csv`
  학생 선택군 단위 요약
- `*_summary_llm.csv`
  하단 합계·창의적 체험활동 등 요약 영역
- `*_readme.md`
  변환 규칙 설명

## 2. 핵심 해석 규칙

### 학기별 숫자

`1-1`, `1-2`, `2-1`, `2-2`, `3-1`, `3-2` 열의 숫자는 해당 학기 편성 학점이다.

이 양식에서는 다음을 같은 의미로 본다.

- 학점
- 주당 시수
- 이수 단위 성격

예를 들어 `공통국어1`의 `1-1` 값이 `3`이면,
`공통국어1은 1학년 1학기에 3학점 편성`된 것이다.

## 3. 병합 셀 처리

원본 표에는 병합 셀이 많다.
스크립트는 병합 셀의 좌상단 값을 병합 범위 전체에 채운다.

예를 들어 `교과군`이 한 번만 표시되어 있어도,
그 아래 과목 행 전체에 같은 `교과군` 값을 채운다.

## 4. 과목유형과 비고성 suffix

`공통`, `일반`, `진로`, `융합`은 과목의 주된 유형이다.

반면 `전문`, `고시외`, `표6` 등은 과목유형이 아니라 별도의 비고성 suffix이다.

예:

- `사물인터넷과 센서제어`
  - 과목유형: `진로`
  - 비고성 suffix: `전문`

따라서 변환 결과에서는 다음처럼 분리한다.

- `과목유형`
- `비고_전문_고시외_표6`

## 5. ○ 표시 처리

원본의 `○` 표시는 `True`로 변환한다.
해당하지 않는 칸은 `False`가 아니라 빈칸으로 둔다.

이유는 `True` 값이 적고, 빈칸이 더 읽기 쉽기 때문이다.

## 6. 학생 선택군 해석

학생 선택군은 특히 주의해야 한다.

원본에는 `택4 (3)`처럼 짧게 적혀 있지만,
같은 `택4 (3)`이라도 운영 학기가 다르면 다른 선택군이다.

따라서 스크립트는 선택군을 다음처럼 코드화한다.

```text
2-1-택4(3)
2-2-택4(3)
3-1-택3(2)
```

즉 선택군 코드는 다음 요소로 구성된다.

```text
학년-학기-선택수(과목당학점)
```

예:

```text
2-1-택4(3)
```

의 의미는 다음과 같다.

- 2학년 1학기 선택군
- 여러 과목 중 4과목 선택
- 과목당 3학점
- 선택군 총 학점은 12학점

## 7. 중점학교 관련 열

과학·정보 중점학교, 사회·인문 중점학교 관련 표시는 별도 boolean 열로 분리한다.

- `과학중점`
- `정보중점`
- `사회중점`
- `인문중점`

각 열은 해당하면 `True`, 아니면 빈칸이다.

## 8. 스크립트 특징 및 전제

이 스크립트는 다음 특징과 전제를 둔다.

- **시트 및 열 자동 감지**: 입력된 파일에서 적절한 교육과정 시트를 자동으로 찾아내고, 헤더의 텍스트 조합을 바탕으로 각 열의 위치를 동적으로 매핑합니다. 따라서 임의의 학교 양식에도 대처할 수 있습니다.
- **파일명 접두사 자동 생성**: 학교명(예: 반곡)과 학년도(예: 2026)를 파일명이나 시트 상단 텍스트에서 감지하여 출력 파일의 접두사(예: `bangok_2026_curriculum`)를 자동으로 구성합니다.
- 과목 행의 개수, 즉 테이블 길이만 달라져도 동작합니다.
- 숨김 시트 `<표5>`, `<표6>`, `<표7>`은 변환하지 않습니다.
  - 이 시트들은 학교별 기록이라기보다 지침 성격이므로 별도 지식 문서로 제공하는 것이 적합합니다.

## 9. 실행 방법

필요 패키지:

```bash
pip install openpyxl
```

실행 예:

```bash
# 기본 실행 (시트 및 파일명 접두사 자동 감지)
python convert_curriculum_to_llm_csv.py "2026학년도 반곡고등학교 입학생 3개년 교육과정 편성표.xlsx"

# 시트명이나 출력 접두사 등을 지정하여 실행하는 경우
python convert_curriculum_to_llm_csv.py "input.xlsx" --sheet "반곡고" --outdir "./out" --prefix "bangok_2026_curriculum"
```

출력 결과:

```text
bangok_2026_curriculum_courses_llm.csv
bangok_2026_curriculum_selection_groups_llm.csv
bangok_2026_curriculum_summary_llm.csv
bangok_2026_curriculum_readme.md
```

## 10. 다른 LLM이 주의할 점

이 CSV를 읽을 때 원본 엑셀의 시각적 구조를 상상하지 말고,
CSV의 각 행과 열을 기준으로 해석해야 한다.

특히 다음을 혼동하지 말 것.

- `과목유형`과 `비고_전문_고시외_표6`은 다른 범주이다.
- 같은 `택4(3)`이라도 학기가 다르면 다른 선택군이다.
- 학기별 숫자는 학점이자 주당 시수로 해석한다.
- 빈칸은 대체로 “해당 없음”이며, `0학점`과는 다르다.